# Book Writing Toolbox — 5 blog posts

BlogPost JSON + markdown + 1200×630 WebP heroes (bottom FreeDailyPro sticker only).

## Posts
See `INDEX.json` for slugs, word counts, and raw hero URLs.

## Format
- `posts/*.json` — BlogPost structure (slug, title, description, publishedAt, readTime, tags, content blocks)
- `posts/*.md` — human-readable companion with image + italic line + FreeDailyPro lead
- `images/*.webp` — branded heroes

## Accuracy
Fact sheet only for Book Writing Toolbox features/pricing. Competitor prices verified from public 2026 sources; recheck before publish.
